# TS & WebAssembly experiments - web app

## Install, build and run

`npm run install`

Copy assets, build (and watch for changes)

`npm run dev`

Run simple dev http server

`npm run serve`

Open http://localhost:8080
